#include "logger.h"

int logger::parse(const char* idata, std::string& level, std::string& time, std::string& log)
{
	if (strlen(idata))
	{

	}
}

int logger::scheduler(const std::string buffer, int cfd) 
{
	//Êı¾İ·ÖÎö
	std::cout << buffer << std::endl;

	return 0;
}

